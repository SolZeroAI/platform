interface FontDescriptor {
  className: string
  variable: string
}

export const geistSans: FontDescriptor = {
  className: "font-sans",
  variable: "font-sans",
}

export const geistMono: FontDescriptor = {
  className: "font-mono",
  variable: "font-mono",
}

export const manrope: FontDescriptor = {
  className: "font-display",
  variable: "font-display",
}
