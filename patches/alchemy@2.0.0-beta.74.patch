diff --git a/lib/Cloudflare/AI/SearchInstance.js b/lib/Cloudflare/AI/SearchInstance.js
--- a/lib/Cloudflare/AI/SearchInstance.js
+++ b/lib/Cloudflare/AI/SearchInstance.js
@@ -418,7 +418,7 @@
             name: output.namespace,
             id: output.instanceId,
         })
-            .pipe(Effect.catchTag(["AiSearchInstanceNotFound", "NamespaceNotFound"], () => Effect.void));
+            .pipe(Effect.catchTag(["AiSearchInstanceNotFound", "NamespaceNotFound", "NotFound"], () => Effect.void));
     }),
 });
 /**
@@ -464,12 +464,13 @@
 const resolveNamespace = (namespace) => namespace ?? "default";
 /**
  * Read an instance by id within its namespace, mapping "gone" to
- * `undefined` — either the instance is missing (`AiSearchInstanceNotFound`,
- * code 7002) or its whole namespace is (`NamespaceNotFound`, code 7063).
+ * `undefined` — either the instance is missing (`AiSearchInstanceNotFound`
+ * or `NotFound`, code 7002) or its whole namespace is (`NamespaceNotFound`,
+ * code 7063).
  */
 const getInstance = (accountId, namespace, id) => aisearch
     .readNamespaceInstance({ accountId, name: namespace, id })
-    .pipe(Effect.catchTag(["AiSearchInstanceNotFound", "NamespaceNotFound"], () => Effect.succeed(undefined)));
+    .pipe(Effect.catchTag(["AiSearchInstanceNotFound", "NamespaceNotFound", "NotFound"], () => Effect.succeed(undefined)));
 const createInstanceId = (id, instanceId) => Effect.gen(function* () {
     return instanceId ?? (yield* createPhysicalName({ id, lowercase: true }));
 });
diff --git a/lib/Cloudflare/Containers/ContainerApplication.d.ts b/lib/Cloudflare/Containers/ContainerApplication.d.ts
--- a/lib/Cloudflare/Containers/ContainerApplication.d.ts
+++ b/lib/Cloudflare/Containers/ContainerApplication.d.ts
@@ -172,6 +172,11 @@
     /** Entrypoint file for the Effect program, typically `import.meta.url`. */
     main: string;
     /**
+     * Bundle `main` as a plain external program and preserve an inline
+     * Dockerfile as the complete image definition.
+     */
+    isExternal?: boolean;
+    /**
      * Environment image for the generated Dockerfile — a plain registry
      * reference, e.g. `"oven/bun:latest"`. Alchemy synthesizes the `FROM` line
      * and appends the statements that copy the bundled program and set the
@@ -291,6 +296,7 @@
  */
 export interface AnyContainerApplicationProps extends ContainerApplicationPropsBase {
     main?: string;
+    isExternal?: boolean;
     image?: string;
     context?: string;
     dockerfile?: string | InlineDockerfile;
diff --git a/lib/Cloudflare/Containers/ContainerBundle.d.ts b/lib/Cloudflare/Containers/ContainerBundle.d.ts
--- a/lib/Cloudflare/Containers/ContainerBundle.d.ts
+++ b/lib/Cloudflare/Containers/ContainerBundle.d.ts
@@ -78,7 +78,7 @@
  * then appends the statements that copy the bundled program and set the
  * entrypoint.
  */
-export declare const buildFinalDockerfile: (envPreamble: string | undefined, runtime: "bun" | "node", external?: string[], autoInstallExternals?: boolean) => string;
+export declare const buildFinalDockerfile: (envPreamble: string | undefined, runtime: "bun" | "node", external?: string[], autoInstallExternals?: boolean, isExternal?: boolean) => string;
 /**
  * Materialize resolved inline `dockerfile` content into a stable,
  * deterministic build-context directory (containing only the Dockerfile) so
diff --git a/lib/Cloudflare/Containers/ContainerBundle.js b/lib/Cloudflare/Containers/ContainerBundle.js
--- a/lib/Cloudflare/Containers/ContainerBundle.js
+++ b/lib/Cloudflare/Containers/ContainerBundle.js
@@ -156,7 +156,10 @@
  * then appends the statements that copy the bundled program and set the
  * entrypoint.
  */
-export const buildFinalDockerfile = (envPreamble, runtime, external = [], autoInstallExternals = true) => {
+export const buildFinalDockerfile = (envPreamble, runtime, external = [], autoInstallExternals = true, isExternal = false) => {
+    if (isExternal && envPreamble) {
+        return `${envPreamble.trim()}\n`;
+    }
     const base = envPreamble ??
         (runtime === "bun" ? "FROM oven/bun:1" : "FROM node:22-slim");
     const runtimeBin = runtime === "bun" ? "bun" : "node";
@@ -379,7 +382,7 @@
     yield* validateContainerImageProps(news);
     const runtime = news.runtime ?? "bun";
     const context = yield* getStableContextDir(process.cwd(), dotAlchemy, `${id}-container`);
-    const dockerfileContent = buildFinalDockerfile(yield* containerEnvPreamble(news), runtime, news.external, news.autoInstallExternals);
+    const dockerfileContent = buildFinalDockerfile(yield* containerEnvPreamble(news), runtime, news.external, news.autoInstallExternals, news.isExternal);
     const [bundle] = yield* Effect.all([
         bundleContainerProgram({
             id,
diff --git a/lib/Cloudflare/Containers/ContainerProvider.js b/lib/Cloudflare/Containers/ContainerProvider.js
--- a/lib/Cloudflare/Containers/ContainerProvider.js
+++ b/lib/Cloudflare/Containers/ContainerProvider.js
@@ -143,7 +143,7 @@
                 external: props.external,
                 build: props.build,
             });
-            const finalDockerfile = buildFinalDockerfile(yield* containerEnvPreamble(props), runtime, props.external, props.autoInstallExternals);
+            const finalDockerfile = buildFinalDockerfile(yield* containerEnvPreamble(props), runtime, props.external, props.autoInstallExternals, props.isExternal);
             const imageHash = (yield* sha256Object({
                 bundleHash,
                 dockerfile: finalDockerfile,
@@ -287,7 +287,7 @@
             }
             const runtime = props.runtime ?? "bun";
             const contextDir = yield* getStableContextDir(process.cwd(), dotAlchemy, `${id}-container`);
-            const finalDockerfile = buildFinalDockerfile(yield* containerEnvPreamble(props), runtime, props.external, props.autoInstallExternals);
+            const finalDockerfile = buildFinalDockerfile(yield* containerEnvPreamble(props), runtime, props.external, props.autoInstallExternals, props.isExternal);
             yield* docker.materialize({
                 context: contextDir,
                 dockerfile: finalDockerfile,
diff --git a/lib/Cloudflare/Workers/WorkerProvider.js b/lib/Cloudflare/Workers/WorkerProvider.js
--- a/lib/Cloudflare/Workers/WorkerProvider.js
+++ b/lib/Cloudflare/Workers/WorkerProvider.js
@@ -947,7 +947,7 @@
         const toRemove = liveAll.filter((d) => !desiredSet.has(d.hostname));
         yield* Effect.all(toRemove.map((d) => workers
             .deleteDomain({ accountId, domainId: d.id })
-            .pipe(Effect.catchTag("DomainNotFound", () => Effect.void))), { concurrency: "unbounded" });
+            .pipe(Effect.catchCause(() => Effect.void))), { concurrency: "unbounded" });
         if (desired.length === 0)
             return [];
         const zoneCache = new Map();
@@ -975,7 +975,7 @@
                 // then recreate it below with the explicitly requested zone.
                 yield* workers
                     .deleteDomain({ accountId, domainId: live.id })
-                    .pipe(Effect.catchTag("DomainNotFound", () => Effect.void));
+                    .pipe(Effect.catchCause(() => Effect.void));
             }
             // Not attached to this Worker — but it could still belong
             // to another Worker. Check before we try to PUT so we can
@@ -3908,7 +3908,7 @@
                             accountId: output.accountId,
                             domainId: d.id,
                         })
-                            .pipe(Effect.catchTag("DomainNotFound", () => Effect.void)),
+                            .pipe(Effect.catchCause(() => Effect.void)),
                     ]
                     : []), { concurrency: "unbounded" });
             }
diff --git a/src/Cloudflare/AI/SearchInstance.ts b/src/Cloudflare/AI/SearchInstance.ts
--- a/src/Cloudflare/AI/SearchInstance.ts
+++ b/src/Cloudflare/AI/SearchInstance.ts
@@ -778,7 +778,7 @@
         })
         .pipe(
           Effect.catchTag(
-            ["AiSearchInstanceNotFound", "NamespaceNotFound"],
+            ["AiSearchInstanceNotFound", "NamespaceNotFound", "NotFound"],
             () => Effect.void,
           ),
         );
@@ -839,15 +839,17 @@

 /**
  * Read an instance by id within its namespace, mapping "gone" to
- * `undefined` — either the instance is missing (`AiSearchInstanceNotFound`,
- * code 7002) or its whole namespace is (`NamespaceNotFound`, code 7063).
+ * `undefined` — either the instance is missing (`AiSearchInstanceNotFound`
+ * or `NotFound`, code 7002) or its whole namespace is (`NamespaceNotFound`,
+ * code 7063).
  */
 const getInstance = (accountId: string, namespace: string, id: string) =>
   aisearch
     .readNamespaceInstance({ accountId, name: namespace, id })
     .pipe(
-      Effect.catchTag(["AiSearchInstanceNotFound", "NamespaceNotFound"], () =>
-        Effect.succeed(undefined),
+      Effect.catchTag(
+        ["AiSearchInstanceNotFound", "NamespaceNotFound", "NotFound"],
+        () => Effect.succeed(undefined),
       ),
     );

diff --git a/src/Cloudflare/Containers/ContainerApplication.ts b/src/Cloudflare/Containers/ContainerApplication.ts
--- a/src/Cloudflare/Containers/ContainerApplication.ts
+++ b/src/Cloudflare/Containers/ContainerApplication.ts
@@ -206,6 +206,11 @@
   /** Entrypoint file for the Effect program, typically `import.meta.url`. */
   main: string;
   /**
+   * Bundle `main` as a plain external program and preserve an inline
+   * Dockerfile as the complete image definition.
+   */
+  isExternal?: boolean;
+  /**
    * Environment image for the generated Dockerfile — a plain registry
    * reference, e.g. `"oven/bun:latest"`. Alchemy synthesizes the `FROM` line
    * and appends the statements that copy the bundled program and set the
@@ -332,6 +337,7 @@
  */
 export interface AnyContainerApplicationProps extends ContainerApplicationPropsBase {
   main?: string;
+  isExternal?: boolean;
   image?: string;
   context?: string;
   dockerfile?: string | InlineDockerfile;
diff --git a/src/Cloudflare/Containers/ContainerBundle.ts b/src/Cloudflare/Containers/ContainerBundle.ts
--- a/src/Cloudflare/Containers/ContainerBundle.ts
+++ b/src/Cloudflare/Containers/ContainerBundle.ts
@@ -220,7 +220,12 @@
   runtime: "bun" | "node",
   external: string[] = [],
   autoInstallExternals = true,
+  isExternal = false,
 ): string => {
+  if (isExternal && envPreamble) {
+    return `${envPreamble.trim()}\n`;
+  }
+
   const base =
     envPreamble ??
     (runtime === "bun" ? "FROM oven/bun:1" : "FROM node:22-slim");
@@ -508,6 +513,7 @@
     runtime,
     news.external,
     news.autoInstallExternals,
+    news.isExternal,
   );
   const [bundle] = yield* Effect.all(
     [
diff --git a/src/Cloudflare/Containers/ContainerProvider.ts b/src/Cloudflare/Containers/ContainerProvider.ts
--- a/src/Cloudflare/Containers/ContainerProvider.ts
+++ b/src/Cloudflare/Containers/ContainerProvider.ts
@@ -234,6 +234,7 @@
             runtime,
             props.external,
             props.autoInstallExternals,
+            props.isExternal,
           );
           const imageHash = (yield* sha256Object({
             bundleHash,
@@ -420,6 +421,7 @@
             runtime,
             props.external,
             props.autoInstallExternals,
+            props.isExternal,
           );
           yield* docker.materialize({
             context: contextDir,
diff --git a/src/Cloudflare/Workers/WorkerProvider.ts b/src/Cloudflare/Workers/WorkerProvider.ts
--- a/src/Cloudflare/Workers/WorkerProvider.ts
+++ b/src/Cloudflare/Workers/WorkerProvider.ts
@@ -1380,7 +1380,7 @@
             toRemove.map((d) =>
               workers
                 .deleteDomain({ accountId, domainId: d.id })
-                .pipe(Effect.catchTag("DomainNotFound", () => Effect.void)),
+                .pipe(Effect.catchCause(() => Effect.void)),
             ),
             { concurrency: "unbounded" },
           );
@@ -1417,7 +1417,7 @@
               // then recreate it below with the explicitly requested zone.
               yield* workers
                 .deleteDomain({ accountId, domainId: live.id })
-                .pipe(Effect.catchTag("DomainNotFound", () => Effect.void));
+                .pipe(Effect.catchCause(() => Effect.void));
             }

             // Not attached to this Worker — but it could still belong
@@ -5273,7 +5273,7 @@
                           domainId: d.id,
                         })
                         .pipe(
-                          Effect.catchTag("DomainNotFound", () => Effect.void),
+                          Effect.catchCause(() => Effect.void),
                         ),
                     ]
                   : [],
